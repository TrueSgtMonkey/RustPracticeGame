/**
the "main" function in rust is always the first function executed

Rust's "style" is to indent with 4 spaces
 */
fn main () {
    // The "!" means that we are calling a macro instead of a function
    // normal function calls do not need this
    println!("Hello, world!");
}